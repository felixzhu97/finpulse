{
  "buildCommand": "pnpm install && pnpm --filter finpulse-admin build",
  "outputDirectory": "apps/admin/dist",
  "installCommand": "pnpm install",
  "framework": null,
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
