{
  "buildCommand": "pnpm turbo build --filter=finpulse-admin",
  "outputDirectory": "apps/admin/dist",
  "installCommand": "pnpm install",
  "framework": null,
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
