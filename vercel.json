{
  "buildCommand": "pnpm install && pnpm --filter web build --configuration production",
  "outputDirectory": "apps/web/dist/web/browser",
  "installCommand": "pnpm install",
  "framework": null,
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
